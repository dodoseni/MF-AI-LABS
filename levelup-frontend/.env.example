# Base URL of the LevelUp backend API (no trailing slash).
# This value is not a secret — it is a public API hostname baked into the frontend bundle.
#
# Copy this file to `.env.local` (untracked, see .gitignore) and adjust as needed:
#   cp .env.example .env.local
#
# Currently deployed dev backend:
VITE_API_BASE_URL=https://levelup-api-dev-dfgugzd7a0fvf4f7.swedencentral-01.azurewebsites.net
